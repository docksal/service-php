; Global PHP settings
; /usr/local/etc/php/conf.d/zzz-20-php.ini
[php]
; Maximum amount of memory a script may consume
memory_limit = 1002M

[mail]
; Mail routed through an SMTP server
sendmail_path = "/usr/bin/msmtp -t --host=example.com --port=25"
